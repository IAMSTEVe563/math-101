#ifndef PLAYER_H\n#define PLAYER_H\n\nclass Player {\npublic:\n    // Attributes\n    int health;\n    int armor;\n    std::vector<int> inventory;\n    float movementSpeed;\n\n    // Constructor\n    Player(int initialHealth, int initialArmor) : health(initialHealth), armor(initialArmor), movementSpeed(0.0f) {}\n\n    // Health management functions\n    void takeDamage(int damage) {\n        int effectiveDamage = damage - armor;\n        if (effectiveDamage < 0) effectiveDamage = 0;\n        health -= effectiveDamage;\n    }\n    bool isAlive() const { return health > 0; }\n\n    // Armor management functions\n    void equipArmor(int newArmor) { armor = newArmor; }\n\n    // Inventory management functions\n    void addItem(int item) { inventory.push_back(item); }\n    void removeItem(int item) {\n        inventory.erase(std::remove(inventory.begin(), inventory.end(), item), inventory.end());\n    }\n\n    // Movement management functions\n    void setMovementSpeed(float speed) { movementSpeed = speed; }\n    float getMovementSpeed() const { return movementSpeed; }\n\n    // Weapon management functions\n    // Function to equip weapon can be added here\n};\n\n#endif // PLAYER_H